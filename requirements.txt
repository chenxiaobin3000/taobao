# Backend dependencies inferred from the Django source code.
Django==5.2.8
django-cors-headers>=4.0,<5.0

# Needed on Windows when using zoneinfo-based time zones such as Asia/Shanghai.
tzdata>=2024.1; sys_platform == "win32"

# OCR dependencies for invoice recognition.
# For CPU installs on Windows, PaddlePaddle may need the official Paddle index:
# python -m pip install paddlepaddle==3.2.0 -i https://www.paddlepaddle.org.cn/packages/stable/cpu/
pypdf>=5.0,<7.0
paddlepaddle==3.2.0
paddleocr>=3.3.0,<4.0
